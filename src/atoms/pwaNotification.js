import { atom } from 'jotai';

export const pageVisitCountAtom = atom(0);
export const postCountAtom = atom(0);
export const installPromptAtom = atom(null);
export const showInstallBannerAtom = atom(false);